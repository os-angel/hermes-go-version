package whatsapp

// webhook.go — stub para el path alternativo via Meta Cloud API (Fase 9).
// Actualmente hermes-go usa bridge.js (Baileys) como transporte primario.
// Este archivo reserva el paquete para una implementacion futura de
// recepcion de mensajes via webhooks de la API oficial de Meta.

// MetaWebhookHandler procesaria eventos HTTP POST de Meta Cloud API.
// No implementado: Fase 9 usa el bridge Baileys como unico transporte.
type MetaWebhookHandler struct{}
